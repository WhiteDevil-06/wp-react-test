# Next.js / Frontend Agent Scope

- Implements React App Router components in `src/app` and `src/components`.
- Maintains strict TypeScript types for all UI props and WP entity models.
- Integrates data fetchers from `src/lib/wordpress/`.
- Manages client interactivity and component state.
