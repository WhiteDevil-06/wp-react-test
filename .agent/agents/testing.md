# Testing Agent Scope

- Validates API integration contracts and fallback states.
- Conducts the Critical Content-Change Test (WP update -> Next.js dynamic render).
- Verifies mobile responsiveness and error boundary behaviors.
