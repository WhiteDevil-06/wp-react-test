# Workflow: Integration Test

1. Verify WordPress API data retrieval.
2. Execute Content Change Validation Test (modify WP data -> observe Next.js render).
3. Test failure states (unreachable WP server, empty response, missing image/text).
