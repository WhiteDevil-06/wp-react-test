# Workflow: Feature Development

1. Define API data requirements and WP schema representation.
2. Implement data access method in `src/lib/wordpress/`.
3. Create React components (Server Component by default, Client for interactive elements).
4. Apply NEXORA AI design system styling and Framer Motion transitions.
