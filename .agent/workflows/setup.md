# Workflow: Project Setup

1. Initialize `.agent/` skills, sub-agent roles, and guidelines.
2. Bootstrap Next.js 14 project with Tailwind CSS and Framer Motion.
3. Configure environment variables (`WORDPRESS_API_URL`).
4. Establish core directory hierarchy.
