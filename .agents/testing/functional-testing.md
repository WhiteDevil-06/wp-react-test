# Functional Testing Suite

## Procedures
1. **Build Verification**: Run `npm run build` to verify zero TypeScript errors or compilation failures.
2. **API Verification**: Test POST payload handling on `/api/enquiry` with valid data, invalid data, missing fields, and honeypot triggering.
3. **Form State**: Test loading states, success confirmation, and error field highlighting on enquiry form.
